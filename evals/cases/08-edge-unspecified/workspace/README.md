# Mini project

parse_limit is called with decimal strings representing positive integers. Handling of other inputs is outside the defined contract.

Run checks with `python -m unittest discover -s . -p "test*.py" -v`.
