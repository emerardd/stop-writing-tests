# Mini project

parse_limit parses positive decimal strings. There is no agreed default, sentinel, or error response for empty input.

Run checks with `python -m unittest discover -s . -p "test*.py" -v`.
