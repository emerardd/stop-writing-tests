# Mini project

total accepts a sequence of integer (price, quantity) pairs and returns their total; an empty sequence returns 0.

Run checks with `python -m unittest discover -s . -p "test*.py" -v`.
