# Mini project

request_timeout returns the configured timeout in seconds, default 30. The task authorizes the new public key and precedence rule.

Run checks with `python -m unittest discover -s . -p "test*.py" -v`.
